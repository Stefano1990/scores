%html{lang: "en"}
  %head
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tag
    %meta{charset: "utf-8"}/
    %meta{content: "IE=edge", "http-equiv" => "X-UA-Compatible"}/
    %meta{content: "width=device-width, initial-scale=1", name: "viewport"}/
    %meta{content: "", name: "description"}/
    %meta{content: "", name: "author"}/
    %title Dashboard Template for Bootstrap
  %body{ class: "#{params[:controller]} #{params[:action]}"}
    .navbar.navbar-inverse.navbar-fixed-top{role: "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{ href: "#" } Scores alpha
        .navbar-collapse.collapse
          %ul.nav.navbar-nav
            %li= link_to "League: BTCC", graphics_path
            %li= link_to "All leagues", graphics_path
            %li= link_to "Profile", '#'
          %ul.nav.navbar-nav.navbar-right
            %li= link_to "Log out", destroy_user_session_path, method: :delete
    .container-fluid
      .row
        - if current_user
          .col-sm-3.col-md-2.sidebar
            %ul.nav.nav-sidebar.leagues-navi
              - if current_league
                - current_league.series.each do |series|
                  - if series_active?(series)
                    %li.active= link_to series.name, league_series_path(series.league,series)
                    - series.seasons.each do |season|
                      %li.lvl2= link_to season.name
                  - else
                    %li= link_to series.name, league_series_path(series,series.league)
                  %li.nav-divider

              -#%li.lvl2= link_to "Season 1"
              -#%li.lvl3= link_to "Results"
              -#%li.lvl3= link_to "Standings"
              -#%li.nav-divider
              -#%li= link_to "Carrera Cup"
              -#%li= link_to "All Graphics".html_safe, graphics_path
              -#%li.new= link_to "New graphic".html_safe, new_graphic_path
              -#- current_user.graphics.each do |league|
              -#  %li.nav-divider
              -#  - unless league.new_record?
              -#    - if graphic_displayed?(league)
              -#      %li.active= link_to league.name, graphic_path(league)
              -#      %li.edit= link_to "Edit #{league.name}", edit_graphic_path(league)
              -#      %li.new= link_to "New results image", new_graphic_result_path(league)
              -#      %li.new= link_to "New standing image", new_graphic_standing_path(league)
              -#      %li= link_to "Result images", graphic_results_path(league)
              -#      %li= link_to "Standing images", graphic_standings_path(league)
              -#    - else
              -#      %li= link_to league.name, graphic_path(league)
        .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
          - flash.each do |key, value|
            = content_tag(:div, value, class: "alert alert-#{key}")
          = yield
